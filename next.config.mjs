/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ["cdn.pixabay.com", "images.unsplash.com", "plus.unsplash.com","videos.pexels.com"],
  },
};

export default nextConfig;
